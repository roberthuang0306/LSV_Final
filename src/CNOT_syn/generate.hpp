#ifndef GENERATE_HPP
#define GENERATE_HPP

#include<iostream>
#include<fstream>
#include<random>
#include<vector>

using namespace std;

void Cgenerate(string fileName, int InputNum, int GateNum);

void Tgenerate(string fileName, int InputNum);

#endif